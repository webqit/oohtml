
/**
 * We expect the following here
 */
const ENV = {
    Window: null,
    Trap: null,
    params: {
        scriptElement: 'script[type="text/scoped-js"]',
        bindMethodName: 'bind',
        innertContexts: [],
        autoHide: true,
        bindCallback: null,
    },
    globals: {},
};

/**
 * @exports
 */
export default ENV;